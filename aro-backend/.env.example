# ARO Backend — Environment Variables
# Copy this to .env and fill in the values

# PostgreSQL connection string from Railway
DATABASE_URL=postgresql://user:password@host:port/database

# Secret key for JWT tokens (use a long random string)
JWT_SECRET=cambia_esto_por_una_clave_muy_secreta_aleatoria_larga

# Admin por defecto (se crea al iniciar). Cámbialo en producción.
DEFAULT_ADMIN_EMAIL=admin@onfire.com
DEFAULT_ADMIN_PASSWORD=cambia_esta_contraseña

# Contraseñas de los admins reales (se usan al correr: node aro-backend/seed.js)
ADMIN_ANDRY_PASSWORD=
ADMIN_OSMAR_PASSWORD=
ADMIN_RUSSEL_PASSWORD=

# Node environment
NODE_ENV=production

# Port (Railway sets this automatically)
PORT=3000

# Frontend URL (for CORS) — set to your Railway frontend URL or '*' for dev
FRONTEND_URL=*
